const AWAITING_KEYWORDS_STATE = "awaiting_keywords";
const AWAITING_TIME_SLOTS_ACTION = "awaiting_time_slots";
const TIME_SLOTS_MESSAGE = "Please enter the timeslots (comma-separated) when you want to receive news updates, e.g., '08:00, 12:00, 18:00'.";
const REMOVING_KEYWORDS_STATE = "removing_keywords";

export {
    AWAITING_KEYWORDS_STATE,
    AWAITING_TIME_SLOTS_ACTION,
    TIME_SLOTS_MESSAGE,
    REMOVING_KEYWORDS_STATE
}
